# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

# ignore all warnings from all pods
inhibit_all_warnings!

target 'ARKitScrum' do
  use_frameworks!
  pod 'Alamofire', '~> 4.5'
end
